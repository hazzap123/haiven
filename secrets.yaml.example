# Haiven - Required Secrets
# Copy this file to secrets.yaml and fill in your values
# secrets.yaml is gitignored and will not be committed

# WiFi (used by ESPHome bedroom sensor)
wifi_ssid: "your_wifi_ssid"
wifi_password: "your_wifi_password"

# ESPHome API encryption key
# Generate with: python3 -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
api_encryption_key: "generate_a_new_key_here"

# ─────────────────────────────────────────────────────────────────────────────
# Sensor Role Mapping
# ─────────────────────────────────────────────────────────────────────────────
# Haiven uses three stable role names throughout all automations and sensors.
# Physical sensor IDs are set in ONE place: haiven_sensor_roles.yaml
# Run `bash scripts/setup.sh` to configure interactively, or edit directly.
#
# Role          Stable alias                          What it monitors
# ──────────────────────────────────────────────────────────────────────────
# activity  →  binary_sensor.haiven_activity_sensor  Kitchen / living area
# bed       →  binary_sensor.haiven_bed_sensor        Bedroom presence
# bath      →  binary_sensor.haiven_bath_sensor       Bathroom motion
#
# Example:
#   activity: event.kitchen_motion
#   bed:      binary_sensor.bedroom_occupancy
#   bath:     binary_sensor.bathroom_motion
